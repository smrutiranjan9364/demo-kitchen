/node_modules
/.next
/out
/build
next-env.d.ts
.env*.local
.DS_Store
*.tsbuildinfo

# Admin fs data store
/data/.store

# Admin-uploaded images (keep the folder, ignore its contents)
/public/uploads/*
!/public/uploads/.gitkeep

# Isolated browser-test build output
/.next-platform-test/

/.next-platform-build/
